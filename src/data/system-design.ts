export type SystemDesignDiagram =
  | "overview"
  | "monolith"
  | "microservices"
  | "comparison"
  | "scale"
  | "load-balancer"
  | "metrics"
  | "cache"
  | "cdn"
  | "sql"
  | "nosql"
  | "index"
  | "replication"
  | "sharding"
  | "fault-tolerance"
  | "consistency"
  | "cap"
  | "communication"
  | "queue"
  | "rate-limit"
  | "state";

export type SystemDesignQuestion = {
  question: string;
  options: string[];
  answer: number;
  explanation: string;
};

export type SystemDesignTopic = {
  slug: string;
  title: string;
  level: "Beginner" | "Intermediate";
  description: string;
  introduction: string;
  keyIdea: string;
  example: string;
  diagram: SystemDesignDiagram;
  diagramLabels: string[];
  questions: [SystemDesignQuestion, SystemDesignQuestion];
};

const q = (question: string, options: string[], answer: number, explanation: string): SystemDesignQuestion => ({ question, options, answer, explanation });

export const systemDesignTopics: SystemDesignTopic[] = [
  { slug: "what-is-system-design", title: "What Is System Design?", level: "Beginner", description: "Think in building blocks, trade-offs, and how a system behaves under real demand.", introduction: "System design is the practice of deciding how software pieces fit together before, and while, you build them. You sketch users, services, data stores, and the paths between them. The goal is not one perfect diagram. It is a design that meets product needs, stays understandable, and keeps working as traffic, data, and failures grow.", keyIdea: "Start with the user request, then follow it through compute, data, and response. Every arrow is a possible delay or failure.", example: "For a food delivery app, design covers mobile requests, restaurant search, order storage, payment, driver updates, and what happens when one service is temporarily unavailable.", diagram: "overview", diagramLabels: ["Users", "API", "Data", "Response"], questions: [q("What does system design primarily describe?", ["How pieces fit and behave", "Only screen colors", "One code function", "A team's org chart"], 0, "System design maps components, interactions, and trade-offs."), q("What should you trace first?", ["A database index", "A user request", "A server brand", "A CSS rule"], 1, "A request journey reveals needed components and failure points.")] },
  { slug: "monolithic-architecture", title: "Monolithic Architecture", level: "Beginner", description: "One deployable application containing most or all product capabilities.", introduction: "A monolith packages an application's user interface, business rules, and data access into one deployable unit. It is often the fastest way to start because local calls are simple and one team can understand the whole product. Its shape is not automatically bad; the challenge appears when one change requires rebuilding, testing, and deploying everything.", keyIdea: "One deployment does not mean messy code. A modular monolith can keep boundaries inside one process.", example: "An early online shop might serve catalog, cart, checkout, and admin from one application connected to one database. A small team can ship it quickly and debug it in one place.", diagram: "monolith", diagramLabels: ["Web app", "Catalog", "Orders", "Database"], questions: [q("What defines a monolith?", ["One deployable unit", "Only one user", "No database", "Many regions"], 0, "A monolith is packaged and deployed as one application."), q("What is a benefit early on?", ["Simple local calls", "No testing needed", "Infinite scaling", "No failures"], 0, "Calls inside one process are straightforward, helping small teams move quickly.")] },
  { slug: "microservices-architecture", title: "Microservices Architecture", level: "Intermediate", description: "Small services own focused capabilities and can be deployed independently.", introduction: "Microservices split a product into independently deployable services, each responsible for a focused business capability. Services communicate over a network and may own their data. This can let teams release and scale parts separately, but it adds operational work: service discovery, observability, retries, network failures, and contracts between teams.", keyIdea: "Microservices trade local simplicity for independent ownership and deployment.", example: "A marketplace could separate identity, catalog, orders, payments, and notifications. A catalog team can scale search without deploying payment code, while both teams must handle network calls and versioned APIs.", diagram: "microservices", diagramLabels: ["Identity", "Catalog", "Orders", "Payments"], questions: [q("Why split services?", ["Independent ownership", "Remove all networks", "Avoid APIs", "Guarantee zero bugs"], 0, "Focused services can be owned, deployed, and scaled independently."), q("What does a split introduce?", ["Network failure modes", "No monitoring", "One process", "Free storage"], 0, "Remote calls add latency, retries, timeouts, and partial failure concerns.")] },
  { slug: "monolith-vs-microservices", title: "Monolith vs Microservices", level: "Intermediate", description: "Compare one deployable unit with independently operated services.", introduction: "The monolith-versus-microservices choice is a trade-off, not a maturity badge. Monoliths reduce moving parts and are usually easier to develop, test, and operate at small scale. Microservices help when teams, domains, or scaling needs are genuinely separate. Starting with clear modules in one application often preserves simplicity while leaving room to split later.", keyIdea: "Choose the smallest architecture that handles today's constraints, not tomorrow's hypothetical traffic.", example: "A five-person startup may keep billing and accounts in one modular app. A large company with separate teams and very different scaling needs may split those capabilities into services.", diagram: "comparison", diagramLabels: ["One app", "Many services", "Simple", "Independent"], questions: [q("Which usually has fewer operational parts?", ["A monolith", "Microservices", "Both always", "Neither"], 0, "One deployable unit generally needs fewer network and deployment systems."), q("When can microservices help most?", ["Separate team ownership", "A tiny script", "No users", "No network"], 0, "Independent teams and domains can benefit from service boundaries.")] },
  { slug: "scalability", title: "Scalability", level: "Beginner", description: "A system's ability to handle more work without collapsing.", introduction: "Scalability describes how a system responds when demand increases: more users, requests, data, or background jobs. It is broader than making one server faster. You identify the bottleneck, add capacity where it matters, and measure whether the system still meets its latency and reliability goals. Good designs scale the busy path without making every path expensive.", keyIdea: "Scale the bottleneck, and define what 'more' means: traffic, storage, users, or work per second.", example: "A photo service may need more upload bandwidth during a holiday, more storage over months, and more image-processing workers when a viral post arrives.", diagram: "scale", diagramLabels: ["More users", "Capacity", "Bottleneck", "Healthy service"], questions: [q("What does scalability address?", ["More work or demand", "Only colors", "A single typo", "Team salaries"], 0, "Scalability is about handling growing workload and resources."), q("What should guide scaling?", ["Measured bottleneck", "Guesswork only", "Server color", "Logo size"], 0, "Measurements show where extra capacity will improve the system.")] },
  { slug: "horizontal-vs-vertical-scaling", title: "Horizontal vs Vertical Scaling", level: "Beginner", description: "Add more machines or make one machine larger.", introduction: "Vertical scaling makes one machine stronger by adding CPU, memory, or faster storage. Horizontal scaling adds more machines and spreads work across them. Vertical scaling is easy to understand but has hardware limits and a larger single failure. Horizontal scaling offers more capacity and resilience, though it requires routing, coordination, and often stateless application design.", keyIdea: "Vertical means scale up. Horizontal means scale out.", example: "A small database may first move to a larger instance. A busy web tier usually adds more identical servers behind a load balancer so traffic can keep flowing when one server is unavailable.", diagram: "scale", diagramLabels: ["Scale up", "Scale out", "Bigger server", "More servers"], questions: [q("What is horizontal scaling?", ["Adding machines", "Adding RAM only", "Deleting users", "Reducing requests"], 0, "Horizontal scaling increases capacity by adding instances."), q("What is a common horizontal requirement?", ["Request distribution", "One giant machine", "No networking", "Manual passwords"], 0, "A router or load balancer distributes work across instances.")] },
  { slug: "load-balancing", title: "Load Balancing", level: "Beginner", description: "Distribute incoming requests across healthy servers.", introduction: "A load balancer sits between clients and a pool of application servers. It chooses where each request should go using rules such as round robin, least connections, or health checks. This prevents one server from becoming a hotspot and lets operators add or remove instances without changing the public address. It is a traffic director, not a replacement for capacity planning.", keyIdea: "One stable entry point can spread work and stop sending traffic to unhealthy instances.", example: "When a shopping site adds servers for a sale, the load balancer sends new browser requests across the pool. If one server fails its health check, it is removed from rotation.", diagram: "load-balancer", diagramLabels: ["Users", "Load balancer", "Server A", "Server B"], questions: [q("What is a load balancer's main job?", ["Distribute requests", "Store every file", "Write UI code", "Encrypt databases"], 0, "It routes incoming work across available servers."), q("Why use health checks?", ["Avoid unhealthy servers", "Increase font size", "Create users", "Remove caching"], 0, "Health checks keep traffic away from instances that cannot serve safely.")] },
  { slug: "latency-vs-throughput", title: "Latency vs Throughput", level: "Beginner", description: "Understand response time versus amount of work completed.", introduction: "Latency is how long one operation takes from request to response. Throughput is how much work a system completes in a period, such as requests per second. A system can have high throughput but slow individual responses, or low latency for one request but poor total capacity. Naming both metrics prevents teams from optimizing the wrong experience.", keyIdea: "Latency asks 'how long?' Throughput asks 'how much?'.", example: "A video encoder may process thousands of files per hour while each file takes minutes. A checkout API needs low latency for a shopper, while its batch analytics pipeline may prioritize throughput.", diagram: "metrics", diagramLabels: ["Request", "Time", "Many requests", "Per second"], questions: [q("What does latency measure?", ["Time per operation", "Work per hour", "Disk size", "User count"], 0, "Latency is elapsed time for an individual operation."), q("What does throughput measure?", ["Completed work over time", "One response's color", "Password length", "CPU brand"], 0, "Throughput measures volume completed during a time window.")] },
  { slug: "caching", title: "Caching", level: "Beginner", description: "Keep frequently needed data closer to the code that requests it.", introduction: "A cache stores a reusable copy of data so future requests can avoid slower work. It can reduce database load and improve response time, but cached data can become stale and must eventually expire or be invalidated. Decide what is safe to cache, how long it can live, and what happens on a miss before adding one.", keyIdea: "Cache hits are fast; cache misses follow the normal path and refill the cache.", example: "A product page can check Redis for a popular item's details first. On a miss, the application reads the database, returns the item, and stores a short-lived copy for the next shopper.", diagram: "cache", diagramLabels: ["Application", "Cache", "Database", "Hit / miss"], questions: [q("What does a cache reduce?", ["Repeated slow work", "All failures", "Every write", "User permissions"], 0, "A cache serves reusable data without repeating the expensive source operation."), q("What is a cache miss?", ["Data not in cache", "A server shutdown", "A valid hit", "A database backup"], 0, "On a miss, the application reads from its source and may refill the cache.")] },
  { slug: "cdn", title: "CDN", level: "Beginner", description: "Serve static and cacheable content from locations near users.", introduction: "A content delivery network places copies of content at edge locations around the world. A user's request can be answered by a nearby edge instead of traveling to the origin server. CDNs are especially useful for images, scripts, stylesheets, video segments, and cacheable API responses. They reduce distance and origin load, but content freshness and cache rules still matter.", keyIdea: "Move bytes closer to users so the origin does less work and the network path is shorter.", example: "A portfolio's images and JavaScript bundles can be cached at edge locations. Someone in Singapore downloads them from a nearby point of presence instead of the origin server in Europe.", diagram: "cdn", diagramLabels: ["User", "Edge", "Edge", "Origin"], questions: [q("Where does a CDN serve content from?", ["Nearby edge locations", "Only one database", "A user's CPU", "A queue"], 0, "CDNs distribute content across edge locations near users."), q("What commonly benefits from a CDN?", ["Static assets", "Private passwords", "Uncommitted code", "Database locks"], 0, "Images, scripts, styles, and other cacheable assets are common CDN content.")] },
  { slug: "sql-databases", title: "SQL Databases", level: "Beginner", description: "Store structured data in related tables with strong query capabilities.", introduction: "SQL databases organize data into tables, rows, and columns with a defined schema. Relationships and constraints help keep records consistent, while SQL lets you filter, join, aggregate, and update data precisely. They are a strong default for transactional systems where correctness matters, especially when several changes must succeed or fail together.", keyIdea: "Tables plus relationships plus transactions make structured business data dependable.", example: "An order system can store customers, orders, and line items in related tables. A transaction can create an order and reserve inventory together, preventing half-finished purchases.", diagram: "sql", diagramLabels: ["Orders", "Customers", "Products", "Relations"], questions: [q("How is SQL data commonly organized?", ["Related tables", "Only files", "Random pixels", "Message topics"], 0, "SQL databases organize structured records in tables and relationships."), q("What helps transactional correctness?", ["Constraints and transactions", "No schema", "Duplicated screens", "CDN edges"], 0, "Constraints and atomic transactions protect business data rules.")] },
  { slug: "nosql-databases", title: "NoSQL Databases", level: "Beginner", description: "Use flexible data models for high scale or access patterns.", introduction: "NoSQL is a family of databases that does not require the traditional relational table model. Document, key-value, wide-column, and graph databases each optimize different access patterns. Many offer flexible schemas and easy distribution, but they still require deliberate modeling. NoSQL is not 'no structure'; the structure is often shaped around the queries an application must serve.", keyIdea: "Pick a NoSQL model based on access pattern, scale, and consistency needs.", example: "A session store can map a session ID directly to a small document in a key-value database. A content feed may use documents shaped for fast reads instead of joining many relational tables.", diagram: "nosql", diagramLabels: ["Key", "Document", "Flexible shape", "Fast access"], questions: [q("What does NoSQL describe?", ["Several non-relational models", "A single database product", "No data", "Only SQL joins"], 0, "NoSQL includes document, key-value, graph, and wide-column families."), q("What should shape a NoSQL model?", ["Access patterns", "Table names only", "CSS classes", "Screen size"], 0, "Modeling around real queries helps NoSQL systems perform predictably.")] },
  { slug: "sql-vs-nosql", title: "SQL vs NoSQL", level: "Intermediate", description: "Choose a data model by constraints, relationships, and access patterns.", introduction: "SQL and NoSQL are tools with different strengths, not rival labels for good and bad databases. SQL often fits relational data, rich queries, and multi-record transactions. NoSQL can fit flexible records, very large distributed workloads, or simple high-volume access patterns. Start with the data relationships and failure requirements, then choose the simplest store that meets them.", keyIdea: "Data shape and access pattern matter more than fashion.", example: "Payments usually favor relational transactions. A high-volume event stream or user session lookup may favor a distributed key-value or wide-column store. One product can responsibly use both.", diagram: "comparison", diagramLabels: ["Relations", "Transactions", "Flexible", "Distribution"], questions: [q("What should drive the choice?", ["Data and access needs", "Popularity alone", "Logo color", "Team size only"], 0, "Requirements and access patterns determine the appropriate trade-offs."), q("Can one product use both?", ["Yes", "Never", "Only offline", "Only with CDNs"], 0, "Polyglot persistence can use each database where it fits best.")] },
  { slug: "database-indexing", title: "Database Indexing", level: "Intermediate", description: "Create lookup structures that make common queries faster.", introduction: "An index is an additional data structure that helps a database find rows without scanning every record. Indexes can dramatically improve reads on frequently filtered or sorted columns. They are not free: they use storage and make writes slower because each change must update the index. Design indexes from real query patterns, then verify them with query plans.", keyIdea: "Indexes accelerate reads by spending extra storage and write work.", example: "If a support dashboard often searches tickets by customer_id and created_at, a matching index can avoid scanning millions of tickets. An unused index only adds maintenance cost.", diagram: "index", diagramLabels: ["Query", "Index", "Rows", "Fast lookup"], questions: [q("What does an index optimize?", ["Finding rows", "Writing UI", "Compressing images", "Routing DNS"], 0, "An index gives the database a faster path to matching records."), q("What is an index cost?", ["Storage and write work", "No cost", "More user passwords", "Less schema"], 0, "Indexes take space and must be maintained on inserts and updates.")] },
  { slug: "database-replication", title: "Database Replication", level: "Intermediate", description: "Keep copies of database data on multiple nodes.", introduction: "Replication copies database changes from one node to one or more other nodes. A primary may accept writes while replicas serve reads or provide failover capacity. Replicas improve availability and read scale, but copying takes time, so a read can briefly lag behind a write. A design must define which reads require the newest value.", keyIdea: "Copies improve resilience and read capacity, but introduce replication lag.", example: "An article service writes new posts to a primary database and serves popular read traffic from replicas. After publishing, the author's own request may be routed to the primary to show the newest version.", diagram: "replication", diagramLabels: ["Primary", "Replica 1", "Replica 2", "Changes"], questions: [q("What does replication create?", ["Database copies", "New UI routes", "Only indexes", "Fewer records"], 0, "Replication maintains copies of data on multiple nodes."), q("What can replicas experience?", ["Lag", "No data", "Guaranteed writes", "Zero network"], 0, "Changes take time to travel, creating possible read-after-write lag.")] },
  { slug: "database-sharding", title: "Database Sharding", level: "Intermediate", description: "Split a large dataset across nodes using a shard key.", introduction: "Sharding partitions one logical dataset across multiple database nodes. Each shard owns a subset of records, often selected by a key such as tenant ID or user ID. This can spread storage and write load beyond one machine. It also makes queries, rebalancing, cross-shard transactions, and uneven 'hot' keys more complicated.", keyIdea: "A shard key decides where each record lives, so choosing it is a major design decision.", example: "A global SaaS platform might place tenants across shards by tenant_id. Requests can route directly to the right shard, while reports spanning every tenant need a more expensive fan-out.", diagram: "sharding", diagramLabels: ["Router", "Shard A", "Shard B", "Shard C"], questions: [q("What does sharding split?", ["Data across nodes", "One request into pixels", "A password", "Only backups"], 0, "Sharding partitions records across multiple database nodes."), q("What determines placement?", ["Shard key", "Font family", "CDN region only", "Random UI state"], 0, "The shard key maps a record to its owning partition.")] },
  { slug: "replication-vs-sharding", title: "Replication vs Sharding", level: "Intermediate", description: "Distinguish copies for resilience from partitions for capacity.", introduction: "Replication keeps the same data on multiple nodes. Sharding divides different records across nodes. Replication helps availability and read scale, while sharding helps a dataset or write workload exceed one machine. Systems often combine them: each shard can have replicas. The distinction matters because each solves a different bottleneck and adds different failure modes.", keyIdea: "Replication copies data; sharding divides data.", example: "A social network can shard users by user ID to spread writes, then replicate every shard for failover and read traffic. Losing one replica is different from losing an entire shard's only copy.", diagram: "comparison", diagramLabels: ["Copies", "Partitions", "Availability", "Capacity"], questions: [q("What does replication do?", ["Copies the same data", "Divides records", "Removes storage", "Changes schemas"], 0, "Replicas contain copies of the same logical data."), q("What does sharding target?", ["Capacity across partitions", "Only failover", "Browser cache", "Request encryption"], 0, "Sharding spreads distinct records or workload across partitions.")] },
  { slug: "fault-tolerance", title: "Fault Tolerance", level: "Intermediate", description: "Keep useful service running when components fail.", introduction: "Fault tolerance means a system continues providing an acceptable service when part of it breaks. Designs use redundancy, health checks, timeouts, retries with limits, graceful degradation, and recovery procedures. It does not mean failures disappear. It means failure is expected, contained, visible, and recoverable without losing more data or overwhelming healthy components.", keyIdea: "Assume components fail; isolate impact and preserve the most important path.", example: "If a recommendation service times out, a shopping site can still show products and checkout with a default recommendation list. The degraded response is better than making every page unavailable.", diagram: "fault-tolerance", diagramLabels: ["Request", "Healthy path", "Failed node", "Fallback"], questions: [q("What does fault tolerance expect?", ["Component failures", "Perfect hardware", "No monitoring", "No retries ever"], 0, "Fault-tolerant systems plan for partial failures rather than assuming perfection."), q("What is graceful degradation?", ["Useful reduced service", "Total shutdown", "More database writes", "A bigger logo"], 0, "A fallback preserves core functionality when an optional part fails.")] },
  { slug: "strong-vs-eventual-consistency", title: "Strong vs Eventual Consistency", level: "Intermediate", description: "Choose whether reads must see the newest write immediately.", introduction: "Consistency describes what value a read is allowed to observe after writes happen. Strong consistency makes a successful read reflect the latest accepted write, which is easier for users to reason about but can cost latency or availability. Eventual consistency allows temporary differences between replicas in exchange for speed and resilience, as long as copies converge when updates stop.", keyIdea: "Strong favors an immediate, shared view; eventual favors availability with a convergence window.", example: "A bank balance generally needs strong guarantees. A social media like count can tolerate a few seconds of disagreement between regions if the experience remains responsive.", diagram: "consistency", diagramLabels: ["Write", "Replica A", "Replica B", "Read"], questions: [q("What can eventual consistency allow?", ["Temporary stale reads", "No convergence", "No replicas", "Only SQL"], 0, "Replicas may briefly disagree before updates propagate."), q("Which often needs stronger consistency?", ["Account balance", "View counter", "Avatar color", "Recommendation order"], 0, "Financial balances usually require users to see a dependable current value.")] },
  { slug: "cap-theorem", title: "CAP Theorem", level: "Intermediate", description: "Reason about consistency, availability, and partitions in distributed systems.", introduction: "CAP theorem says that during a network partition, a distributed system must choose between consistency and availability for the affected operation. A partition means nodes cannot reliably communicate; it is not an optional product setting. Systems make different choices by feature: a checkout may reject uncertain writes, while a feed may serve a slightly stale response and stay available.", keyIdea: "CAP trade-offs become unavoidable when communication between nodes breaks.", example: "If two regions lose their connection, a payment system may pause writes to avoid double spending. A news feed may keep serving cached posts in both regions and reconcile later.", diagram: "cap", diagramLabels: ["Consistency", "Availability", "Partition", "Trade-off"], questions: [q("When does CAP force a choice?", ["During a network partition", "During every local read", "Only in SQL", "When CSS loads"], 0, "CAP's trade-off concerns distributed systems during partitioned communication."), q("What can an available system do during uncertainty?", ["Serve a response", "Always show latest globally", "Avoid all data", "Guarantee no conflict"], 0, "Availability means continuing to respond, potentially with a trade-off in consistency.")] },
  { slug: "synchronous-vs-asynchronous-communication", title: "Synchronous vs Asynchronous Communication", level: "Intermediate", description: "Compare waiting for a response with handing work off for later.", introduction: "Synchronous communication keeps the caller waiting for a response before it continues. It is direct and useful when the next step needs an immediate result, but slow or failed dependencies extend the request. Asynchronous communication lets the caller hand work to another component and continue, often through a queue. It improves decoupling but needs status tracking, retries, and failure handling.", keyIdea: "Synchronous means wait now; asynchronous means continue and finish later.", example: "A checkout request may synchronously authorize payment, then asynchronously send a receipt email. Email delivery should not make a successful purchase fail because a mail provider is slow.", diagram: "communication", diagramLabels: ["Caller", "Wait", "Hand off", "Worker"], questions: [q("What does synchronous work do?", ["Waits for a response", "Always uses a queue", "Never returns", "Runs offline only"], 0, "The caller blocks its next step until the dependency responds or times out."), q("Why use asynchronous work?", ["Decouple slow tasks", "Guarantee instant results", "Remove retries", "Avoid all storage"], 0, "Async handoff lets the main request finish while separate work completes later.")] },
  { slug: "message-queues", title: "Message Queues", level: "Intermediate", description: "Buffer work between producers and consumers.", introduction: "A message queue stores work until a consumer is ready to process it. Producers and consumers do not need to run at the same speed or at the same time. Queues absorb bursts, isolate failures, and make background processing easier to scale. Reliable systems define acknowledgement, retry, visibility timeout, ordering, and what happens to messages that repeatedly fail.", keyIdea: "A queue turns a direct dependency into durable work waiting to be handled.", example: "When a user uploads a photo, the API can place an image-processing message on a queue and respond quickly. Worker processes resize images later, retrying temporary failures without blocking the upload.", diagram: "queue", diagramLabels: ["Producer", "Queue", "Worker 1", "Worker 2"], questions: [q("What does a queue buffer?", ["Work messages", "CSS styles", "Database schemas", "User passwords"], 0, "Queues hold messages until consumers can process their work."), q("Why acknowledge messages?", ["Track successful processing", "Change colors", "Create replicas", "Skip failures"], 0, "Acknowledgements tell the queue a message was handled and need not be redelivered.")] },
  { slug: "rate-limiting", title: "Rate Limiting", level: "Intermediate", description: "Control how much traffic a client can send in a time window.", introduction: "Rate limiting caps requests from a user, token, IP, or service over time. It protects capacity, reduces accidental overload, and makes abuse harder. Common algorithms include fixed windows, sliding windows, and token buckets. A useful limit has a clear response, such as HTTP 429, and considers legitimate bursts, distributed clients, and whether limits are shared across instances.", keyIdea: "Limit work before overload, and tell callers when to try again.", example: "A public search API may allow 60 requests per minute per API key. When a client exceeds that budget, the gateway returns 429 and a retry hint instead of letting every request degrade the service.", diagram: "rate-limit", diagramLabels: ["Client", "Gate", "Budget", "API"], questions: [q("What does rate limiting control?", ["Request rate", "Database schema", "Screen width", "Replica content"], 0, "It limits how frequently a client or identity can send work."), q("What status commonly signals a limit?", ["429", "200", "301", "404"], 0, "HTTP 429 Too Many Requests communicates that a rate limit was exceeded.")] },
  { slug: "stateful-vs-stateless-architecture", title: "Stateful vs Stateless Architecture", level: "Intermediate", description: "Understand where user session state lives between requests.", introduction: "A stateless service does not rely on a particular server remembering previous requests; each request carries or retrieves what it needs. Stateful services keep session or workflow information in local memory or another tied-to-instance store. Stateless application servers are easier to add, replace, and route around. Stateful designs can be useful, but need sticky routing or shared durable state when instances change.", keyIdea: "Stateless servers are interchangeable because state lives outside the request process.", example: "A stateless API can verify a token and read a shared session store no matter which instance receives the request. A stateful chat connection may stay on one server while a shared broker coordinates events.", diagram: "state", diagramLabels: ["Request", "Server A", "Shared state", "Server B"], questions: [q("What makes a service stateless?", ["No local request dependency", "No data anywhere", "One server only", "No authentication"], 0, "A stateless instance can handle a request without private prior context."), q("Why is statelessness useful for scaling?", ["Instances are interchangeable", "It removes all storage", "It prevents networking", "It guarantees no bugs"], 0, "Any healthy instance can receive the next request, simplifying scaling and failover.")] },
];

export const systemDesignCourseSlug = "system-design";
export const getSystemDesignTopic = (slug: string) => systemDesignTopics.find((topic) => topic.slug === slug);
